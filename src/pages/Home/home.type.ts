export type OrbitProps = {
  radius?: number;
  duration?: number;
  direction?: "clockwise" | "counter-clockwise";
  startAngle?: number;
  className?: string;
  children: React.ReactNode;
};

export type aboutPageColors = {
  varient: string;
  quotation: string;
  headOne: string;
  bgColor: string;
};
